Project Structure
```commandline
yaml-wandb-args-guide/
├── config.yaml        # YAML configuration file
├── main.py            # Entry point of the program
├── model.py           # Defines the neural network class
├── trainer.py         # Contains training logic
├── utils.py           # Utility functions for YAML parsing and argparse
├── requirements.txt   # Required Python libraries
```

```
yaml-wandb-args-guide_with_sweeping_hyperparameters/
├── sweep_config.yaml        # YAML configuration file
├── sweep_main.py            # Entry point of the program
├── model.py           # Defines the neural network class
├── swwep_trainer.py         # Contains training logic
├── utils.py           # Utility functions for YAML parsing and argparse
├── requirements.txt   # Required Python libraries
```